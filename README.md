# Phenomattic Solutions

Static, self-contained copy of the public page at `phenomattic.com`, prepared for GitHub Pages.

The site has no build step. GitHub Pages can publish directly from the repository root on the `main` branch.
